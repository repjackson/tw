if is_user
    .ui.dividing.header My Courses
else    
    .ui.dividing.header Course Enrollments
.ui.list
    if isInRole 'sol'
        .item
            a.ui.header(href="/sol") Source of Light
    if isInRole 'sol_demo'
        .item
            a.ui.header(href="/sol") Source of Light (demo)
    each my_courses
        .item
            a.ui.header(href="/course/#{_id}") #{title}