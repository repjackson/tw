template(name='view_journal')
    a.ui.basic.button(href='/journal')
        +icon48 name='double-left'
        strong Journal
    .ui.container.document_container
        h1.ui.center.aligned.header View Journal Entry
        .ui.stackable.grid
            .ten.wide.column
                +parent_doc_segment
                .ui.basic.segment
                    |!{content}
                if published
                    +comments
                +doc_matches
                if watson
                    +analyzed_watson_keywords
                +child_docs
            .five.wide.column
                .ui.header
                    |by
                    img.ui.avatar.image(src="{{c.url author.profile.image_id width=50 height=50 gravity='face' crop='fill'}}")
                    |#{author.name}
                    with author
                        +staus_indicator
                    h3.ui.sub.header #{when}
                if is_author
                    +call_watson
                if watson
                    +doc_sentiment
                    +doc_emotion
                    +watson_keywords
                +view_tags_sidebar
                .ui.dividing.header Actions
                +bookmark_button
                +reflect_button
                unless is_author
                    +resonate_button
                    +thanks_button
                +resonates_list
                +toggle_doc_read
                +read_by
                +published
                .ui.divider
                if is_author
                    +edit_link_button
