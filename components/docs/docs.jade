template(name='docs')
    .ui.container
        +doc_cloud
        each docs
            +doc_view
        if isInRole 'admin'
            #add.ui.basic.button
                i.red.configure.icon
                | + Doc

        


template(name='doc_view')
    .ui.text.container.basic.segment
        a(href="/doc/view/#{_id}")
            .image
                img.ui.large.centered.image(src="{{c.url image_id }}")
        .description
            |!{body}
        // .meta
        //     each tags
        //         .ui.circular.small.button.tag(class=tag_class) #{this}
        if can_edit
            .edit.ui.circular.basic.large.button
                i.red.configure.icon
                |Edit
