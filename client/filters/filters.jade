
template(name='tag_filter')
    .ui.left.icon.fluid.input
        i.search.icon
        +inputAutocomplete settings=settings id="search" class="" autocomplete="off" placeholder='Search tags...'
    if selected_tags
        .ui.dividing.header Selected Tags
        #clear_tags.ui.teal.icon.circular.button
            i.remove.icon
        each selected_tags
            .unselect_tag.ui.teal.circular.button
                strong #{this}
    .ui.hidden.fitted.divider
    .ui.dividing.header Browse Tags
    each tags
        .ui.basic.circular.button.select_tag(class=cloud_tag_class) 
            | #{this.name}
            // if isInRole 'admin'
            //     small #{this.count}








template(name='username_filter')
    .ui.dividing.header Browse Authors
    if selected_author_ids
        #clear_authors.ui.teal.icon.circular.button
            i.remove.icon
        each selected_author_ids
            .unselect_author.ui.teal.circular.button
                strong #{this}
    each author_tags
        .ui.basic.circular.button.select_author(class=cloud_tag_class) 
            | #{this}
            // if isInRole 'admin'
            //     small #{this.count}
