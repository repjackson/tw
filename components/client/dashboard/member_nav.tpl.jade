.ui.stackable.two.item.menu
    .ui.text.container
        a.item(href="/dashboard" class="{{isActivePath '/dashboard'}}")
            i.dashboard.icon
            |Dashboard
            // a.item(href='/articles' class="{{isActivePath '/articles'}}")
            //     i.newspaper.icon
            //     | Articles
            // a.item(href='/courses' class="{{isActivePath '/courses'}}")
            //     i.university.icon
            //     | Courses
        //     a.item(href='/tests' class="{{isActivePath '/tests'}}")
        //         i.edit.icon
        //         | Tests
        // a.item(href='/people' class="{{isActivePath '/people'}}")
        //     i.users.icon
        //     | Member Portal
        // a.item(href='/colors' class="{{isActivePath '/colors'}}")
        //     i.paint.brush.icon
        //     | Colors
        a.item(href="/profile/#{currentUser._id}" class="{{isActiveRoute name='view_profile'}}")
            if currentUser.profile.image_id
                img.ui.avatar.image(src="{{c.url currentUser.profile.image_id width=50 height=50 gravity='face' crop='fill'}}")
            else
                i.user.icon
            // if currentUser.name
            //     | #{currentUser.name}
            // else
            | Profile
        // a.item(href='/account' class="{{isActivePath '/account'}}")
        //     i.options.icon
        //     | Account
